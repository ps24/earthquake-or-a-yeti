import EStyleSheet from 'react-native-extended-stylesheet';

const styles = {
  common: {
    alignSelf: 'center',
    width: '100%',
    height: '100%'
  },
};

export default EStyleSheet.create({
  background: styles.common,
  top: styles.common,
});